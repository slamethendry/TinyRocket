<a
  href="#top"
  id="button"
  class="active fixed -bottom-[10px] right-[30px] w-[65px] h-[65px] bg-[#ff00] border-none rounded-[50%] scale-100 overflow-hidden transition-transform duration-[0.15s] flex items-center justify-center cursor-pointer opacity-0 z-20"
>
  <div
    class="relative w-[85%] h-[85%] rounded-[50%] z-10 overflow-hidden backToTopImgWrap"
  >
    <img src="/assets/img/up.png" alt="" class="backFirstImg" />
    <img src="/assets/img/up.png" alt="" class="backLastImg" />
  </div>
</a>

<style>
  #button::after {
    width: 85%;
    height: 85%;
    background-color: rgba(0, 0, 0, 0.3);
    border-radius: 50%;
    position: absolute;
    content: "";
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    transition: 0.15s;
  }

  #button img:first-child {
    transform: translate(-50%, -50%);
  }

  #button img:last-child {
    transform: translate(-50%, 170%);
  }

  #button:hover::after {
    width: 100%;
    height: 100%;
    background-color: #3857f1;
  }

  #button:hover img:first-child {
    transform: translate(-50%, -250%);
  }

  #button:hover img:last-child {
    transform: translate(-50%, -50%);
  }

  #button img {
    width: 20px;
    position: absolute;
    left: 50%;
    top: 50%;
    transition: 0.15s;
  }

  #button {
    animation: blur 0.5s linear both;
    animation-timeline: scroll();
    animation-range: 20px 100px;
  }

  @keyframes showButton {
    to {
      opacity: 1;
    }
  }

  #button.active {
    animation: toggleActive 0.5s linear both;
    animation-timeline: scroll();
    animation-range: 20px 100px;
  }

  @keyframes toggleActive {
    to {
      opacity: 1;
      bottom: 30px;
    }
  }
</style>
